# 《老板鱼来了》ChatGPT / Codex 交接与架构规范整合版

> 项目：老板鱼来了 / Boss Fish Is Coming  
> Git 地址：https://github.com/guo276689-star/boss-fish.git  
> 当前阶段：v0.3 成长与收集动力开发阶段（部分完成）
>
> 当前重点：在不破坏 420×260 小窗口体验和现有业务的前提下，逐项完成并验收 v0.3 子需求。
>
> 本文件用途：新开 ChatGPT 会话、Codex 新会话、代码验收、目录责任划分、未来后端规范预留。

---

## 0. 新开会话交接提示词

把下面这段直接发给新的 ChatGPT 会话：

```text
你现在接手我的独立小游戏项目《老板鱼来了》。

项目地址：
https://github.com/guo276689-star/boss-fish.git

项目类型：
Windows 小窗口 Electron 放置钓鱼游戏。

技术栈：
- Electron
- HTML
- CSS
- JavaScript
- Canvas
- JSON
- localStorage

当前项目不是后端项目，没有服务器、数据库、账号系统、联网功能。
如果我提到“后端架构规范”，请理解为：
1. 当前 Electron 单机项目的分层架构规范；
2. 未来如果引入后端时的预留规范；
3. 不能凭空假设现在已经有后端。

当前已合并 main 的进度：
- v0.1：420×260 小窗口、Canvas 鱼塘、钓鱼、金币、localStorage 存档、图鉴、商店、报表模式。
- v0.1.1：迷你圆形悬浮按钮，可拖动，有鱼上钩时显示感叹号。
- v0.2：功能页改造、今日小委托、稀有鱼反馈、20 种鱼、猫猫简单反应。
- v0.2.1 / v0.2.1.1：Web Audio API 真实轻音效，默认静音，用户开启后播放。
- v0.2.2：静态像素美术接入，包括鱼塘背景、猫猫、鱼漂和稀有提示图标。
- v0.2.3：小窗口 UI 可读性优化。
- v0.2.4：今日委托模板扩展。
- v0.2.5：新手提示、委托文案、稀有提示文案和报表伪装文案打磨。

当前开发分支：
- v0.3-progression-pack。
- 已有提交：摸鱼等级与称号、图鉴详情、8 个本地徽章、商店当前/下一级效果说明。
- 尚未实现：今日总结面板；当前没有 todayStats 存档字段。
- v0.3 尚未合并 main，尚未创建稳定 tag。

当前分支策略：
- 从 v0.3 起，一个大版本使用一个小里程碑分支。
- 一个大版本可包含 3～6 个同主题小需求。
- 每个需求建议单独 commit，必须单独列验收结果。
- 高风险功能继续使用独立分支。
- 不允许把互相无关的功能混在同一版本。

请你之后所有建议都遵守：
1. 不要一次加大系统。
2. 只处理当前明确批准的版本范围。
3. 测试通过再合并 main。
4. 没有证据不能说完成。
5. 未验证项必须明确标注“未验证”。
6. 如果修改开发规则、目录结构、存档结构、任务规则、鱼类字段、升级字段、IPC API、窗口尺寸或错误处理格式，必须同步更新文档。
7. 如果让 Codex 改代码，必须先让它说明会改哪些文件、为什么改、怎么验收。
8. 如果涉及架构，必须说明该文件属于框架机制还是项目自定义业务。
9. 小里程碑中的每个需求建议单独 commit，并且必须单独列验收结果。
10. 高风险功能仍然必须单独分支，互相无关的功能不能混入同一版本。
11. 当前不做后端、账号、联网、数据库、排行榜、Steam 成就、复杂宠物养成、地牢、合成、托盘、全局快捷键或复杂动画系统。
12. 当前继续以 Electron 单机小游戏为准。
13. 每个子需求都要说明是否影响金币、今日委托、图鉴、商店、音效和迷你模式。
14. 如果涉及 localStorage 字段，必须说明默认值、旧存档迁移、reset save 行为和重启保留行为。
15. 没有运行证据不得说完成；没有截图或日志不得说 UI 正常；没有 Git commit 不得说已存档；没有手动测试不得说测试通过。

请先根据以上信息，帮我判断下一步最小可行迭代，不要直接写代码。
```

---

## 1. 当前项目定位

### 1.1 一句话定位

```text
一个能放在 Windows 桌面角落的办公室主题放置钓鱼小挂件。
```

### 1.2 当前核心体验

```text
挂在桌面角落
等待鱼上钩
点击收竿
收集办公室主题鱼
完成今日委托
偶尔切成迷你圆形按钮
```

### 1.3 当前不做的方向

```text
不做大型游戏
不做后端
不做联网
不做账号系统
不做数据库
不做排行榜
不做 Steam 成就
不做复杂宠物养成
不做地牢
不做合成
不做托盘隐藏
不做全局快捷键
不做复杂动画系统
不假设当前项目存在服务器或后端
```

---

## 2. 当前完成进度记录

### 2.1 Git 信息

| 项目 | 内容 |
|---|---|
| Git 仓库 | https://github.com/guo276689-star/boss-fish.git |
| 当前检出分支 | `v0.3-progression-pack` |
| 当前开发提交 | `6f12d48 add v0.3 progression pack` |
| 当前主线 | `main`，本地记录为 `622ddbf merge v0.2.5 polish pack` |
| v0.3 是否合并 main | 否；当前 `main` 不包含 `6f12d48` |
| v0.3 是否已有稳定 tag | 否；当前 tag 列表中没有 v0.3 tag |
| 最近稳定 tag | `v0.2.4-stable` |
| v0.2.5 是否已有稳定 tag | 否；当前 tag 列表中没有 v0.2.5 tag |

> 以上 Git 状态在 2026-06-12 通过本地 `git branch`、`git log` 和 `git tag` 核对。远端 GitHub 页面本次未单独人工核验；远端显示差异时以最新 Git 证据为准。

---

### 2.2 功能进度表

| 版本 | 实际内容 | Git 状态 | 本次文档核验结论 |
|---|---|---|---|
| v0.1 / v0.1.1 / v0.2 | 基础钓鱼、迷你模式、今日委托、稀有反馈、20 种鱼、猫猫反应 | 已进入后续主线 | 历史运行和手动测试本次未重跑 |
| v0.2.1 / v0.2.1.1 | Web Audio API 真实轻音效，默认静音 | 音效代码见 `ad0ffb8`，随 v0.2.2 进入 `main` | `v0.2.1-stable` 指向 `a5f1a07`，该提交没有 `src/sound.js`，不能作为真实音效稳定 tag；声音听感未重新人工验证 |
| v0.2.2 | 静态像素背景、猫猫、鱼漂、稀有提示图标 | 已合并 `main`；tag `v0.2.2-stable` | 代码与资源路径已核对；UI 截图未重新验证 |
| v0.2.3 | UI 可读性优化 | 已合并 `main`；tag `v0.2.3-stable` | commit/tag 已核对；420×260 视觉回归未重新验证 |
| v0.2.4 | 今日委托模板扩展 | 已合并 `main`；tag `v0.2.4-stable` | commit/tag 已核对；每日随机与领奖回归未重新验证 |
| v0.2.5 | 新手提示、委托文案、稀有提示文案、报表文案打磨 | 已合并 `main`；无 v0.2.5 稳定 tag | merge 记录已核对；完整 UI 手动回归未重新验证 |
| v0.3 | 摸鱼等级与称号 | 分支 commit `ff2bc91` | 已提交；运行与 UI 验收未在本次文档更新中重做 |
| v0.3 | 图鉴详情增强 | 分支 commit `c6abc77` | 已提交；点击详情和 20 条鱼显示未重新手动验证 |
| v0.3 | 8 个本地徽章 | 分支 commit `517da95` | 已提交；各徽章解锁条件未重新逐项手动验证 |
| v0.3 | 商店当前/下一级效果说明 | 分支 commit `6f12d48` | 已提交；购买前后显示未重新手动验证 |
| v0.3 | 今日总结面板 | 未实现 | 不得写为完成 |
| v0.3 | `todayStats` 存档字段 | 未新增 | 当前不存在迁移、reset save 或重启保留实现 |
| v0.3 | 合并与稳定 tag | 未合并、未打 tag | 需求包仍为部分完成 |

### 2.3 当前 tag 记录

2026-06-12 本地仓库可见 tag：

```text
v0.1-playtest-pass
v0.2-daily-quests-fixed
v0.2-fish-20
v0.2-rare-feedback
v0.2-stable
v0.2.1-stable
v0.2.2-stable
v0.2.3-stable
v0.2.4-stable
```

注意：

```text
v0.2.1-stable 指向 a5f1a07，该提交没有 src/sound.js。
真实音效代码见 ad0ffb8，并随 v0.2.2 合并进入 main。
当前没有 v0.2.5-stable 或 v0.3-stable。
不得仅根据 tag 名称推断功能内容，必须同时核对 tag 指向的提交。
```

---

## 3. 验收规范：必须有证据

### 3.1 AI / Codex 不能再说“已完成”但没有证据

以后所有功能验收必须遵守：

```text
没有运行证据，不得说完成。
没有截图或日志，不得说 UI 正常。
没有 Git commit，不得说已存档。
没有手动测试，不得说测试通过。
不确定的内容必须标注“未验证”。
```

### 3.2 每次验收必须输出的证据

| 证据类型 | 必须内容 |
|---|---|
| 运行证据 | 执行过 `npm start`，说明是否成功启动 |
| 文件证据 | 列出修改过的文件 |
| 功能证据 | 每个功能对应一条实际测试结果 |
| UI 证据 | 涉及 UI 的功能需要截图或明确人工观察结果 |
| 数据证据 | 涉及存档、金币、任务进度，要说明 localStorage 或界面变化 |
| Git 证据 | commit hash 或 commit message |
| 未验证项 | 明确列出未验证内容 |
| 风险项 | 明确列出可能的逻辑冲突 |

### 3.3 标准验收输出格式

```text
## 验收结果

结论：
- 通过 / 未通过 / 部分通过

已验证：
1. [功能] 证据：...
2. [功能] 证据：...

未验证：
1. [原因] ...
2. [原因] ...

发现的问题：
1. 严重度：高 / 中 / 低
   问题：
   证据：
   最小修复建议：

逻辑冲突检查：
1. 是否影响金币：是/否
2. 是否影响今日委托：是/否
3. 是否影响图鉴：是/否
4. 是否影响商店：是/否
5. 是否影响音效：是/否
6. 是否影响迷你模式：是/否

localStorage 变更（如涉及）：
- 新字段及默认值：
- 旧存档迁移：
- reset save 行为：
- 重启保留行为：

Git 存档：
- 分支：
- commit：
- 是否已 push：
```

### 3.4 规避逻辑冲突的规则

#### 今日委托相关

```text
金币任务必须使用本次实际 earnedCoins。
不要在委托模块里重新计算金币。
catch_rarity 必须使用 fish.rarity。
claimed 后不能重复领奖。
reset save 必须重置任务。
```

#### 稀有鱼反馈相关

```text
只能读 fish.rarity。
不能修改 fish.json。
不能影响金币。
不能影响今日委托。
不能影响图鉴解锁。
```

#### 猫猫反应相关

```text
只影响 Canvas 绘制状态。
不能影响 fishing.js 的上钩状态。
不能影响 save.js 的金币和存档。
不能影响 daily-quests.js。
```

#### 迷你模式相关

```text
只改变窗口显示模式和尺寸。
不能暂停钓鱼逻辑，除非明确设计。
不能丢失上钩状态。
不能让普通模式尺寸偏移。
```

### 3.5 大版本需求包验收

从 v0.3 起，大版本需求包不能只给一个笼统结论。每个子需求必须单独列出：

```text
1. 子需求名称和对应 commit（如已提交）。
2. 自动检查结果。
3. 运行证据。
4. UI 截图、日志或明确的人工观察结果。
5. 是否影响金币、今日委托、图鉴、商店、音效、迷你模式。
6. 未验证项和已知风险。
```

如果子需求新增或修改 localStorage 字段，还必须单独说明：

```text
字段默认值
旧存档迁移方式
reset save 行为
重启后的保留行为
损坏或缺字段时的回退方式
```

只有全部子需求都有证据并完成综合回归后，才能把整个需求包写为“通过”。只有代码或 commit 证据时，只能写“已提交”或“代码存在”，不能代替运行和手动验收。

---

## 4. 当前目录责任表

> 说明：以下是基于当前 Electron 单机项目的目录责任划分。  
> “框架来源”分为：Electron 机制、Web 标准机制、Node/npm 机制、项目自定义。  
> 当前项目没有后端框架和数据库框架。

| 目录 / 文件 | 主要职责 | 以后应该放什么 | 禁止放什么 | 示例文件 | 框架机制 / 来源 |
|---|---|---|---|---|---|
| `package.json` | 项目依赖、npm scripts、Electron 启动配置 | scripts、依赖版本、项目元信息 | 游戏业务逻辑、鱼类数据、存档数据 | `package.json` | Node/npm 机制 |
| `package-lock.json` | 锁定依赖版本 | 自动生成的依赖锁 | 手动业务配置 | `package-lock.json` | Node/npm 机制 |
| `main.js` | Electron 主进程入口，窗口创建，安全窗口配置，IPC 主进程处理 | BrowserWindow 配置、窗口尺寸切换、最小必要 ipcMain | Canvas 绘制、钓鱼规则、图鉴逻辑、任务规则 | `main.js` | Electron 机制 |
| `preload.js` | 安全桥接主进程和渲染进程 | `contextBridge.exposeInMainWorld` 暴露白名单 API | 业务规则、大量状态、DOM 操作 | `preload.js` | Electron 安全机制 |
| `index.html` | 页面结构入口 | 根容器、canvas、主 UI 容器、页面引用 | 大量 JS 业务逻辑、鱼类数据 | `index.html` | Web 标准 |
| `styles/` | 全局样式、页面样式、模式样式 | 主界面、功能页、图鉴、商店、报表、迷你按钮样式 | 业务判断、金币计算、任务计算 | `styles/main.css`, `styles/disguise.css` | Web CSS / 项目自定义 |
| `src/renderer.js` | 渲染进程启动入口，模块初始化，UI 事件协调 | 初始化 game/save/shop/bestiary/quests/disguise/sound/progression，绑定页面入口 | 深层业务计算、大量 Canvas 绘图细节 | `src/renderer.js` | 项目自定义 |
| `src/navigation.js` | 渲染进程页面切换协调 | 主界面、功能页、图鉴、商店、成长页等页面显示切换 | 金币、任务、抽鱼、升级等业务规则 | `src/navigation.js` | 项目自定义 |
| `src/game.js` | Canvas 游戏画面、鱼塘、猫猫、鱼漂、视觉状态 | 绘制鱼塘、静态图片及失败回退、猫猫反应、上钩视觉、稀有提示显示 | localStorage 细节、任务奖励发放、商店购买计算 | `src/game.js` | 项目自定义 |
| `src/fishing.js` | 钓鱼状态机、上钩计时、按权重抽鱼 | fish.json 加载、上钩等待、fishBiting 状态、随机鱼 | 金币保存、图鉴 UI、商店 UI、任务领奖 | `src/fishing.js` | 项目自定义 |
| `src/save.js` | 本地存档读写、默认存档、钓鱼收益入账 | `loadSave`, `saveGame`, `resetSave`, `addCatchToSave` | Canvas 绘制、DOM 大量渲染、Electron 窗口控制 | `src/save.js` | 项目自定义 / localStorage |
| `src/bestiary.js` | 图鉴 UI 和解锁显示 | fish 列表展示、已钓/未钓状态 | 金币计算、任务奖励、窗口尺寸控制 | `src/bestiary.js` | 项目自定义 |
| `src/shop.js` | 商店 UI 和升级购买 | 升级价格、购买按钮、等级显示 | 新鱼数据、Canvas 绘制、任务奖励领取 | `src/shop.js` | 项目自定义 |
| `src/disguise.js` | 报表模式 UI 切换 | 报表页显示/返回 | 真实公司信息、账号、权限、钓鱼规则 | `src/disguise.js` | 项目自定义 |
| `src/daily-quests.js` | 今日委托业务规则和 UI | 任务生成、进度更新、领奖、日期刷新 | 钓鱼随机、金币基础规则重新计算、鱼类配置 | `src/daily-quests.js` | 项目自定义 |
| `src/sound.js` | 轻音效开关与 Web Audio API 提示音 | 默认静音、开关持久化、上钩/收竿/稀有鱼/领奖音效 | BGM、复杂音频引擎、金币或任务规则 | `src/sound.js` | Web Audio API / 项目自定义 |
| `src/progression.js` | v0.3 成长页派生展示 | 根据已有统计计算等级、称号和本地徽章显示 | 修改钓鱼统计、发金币、改变图鉴解锁 | `src/progression.js` | 项目自定义 |
| `data/` | 静态游戏数据 | fish、upgrades、未来可加静态配置 | 存档、用户数据、运行时状态 | `data/fish.json`, `data/upgrades.json` | 项目自定义数据 |
| `assets/images/` | 静态像素图片资源 | 鱼塘背景、猫猫、鱼漂、稀有提示图标 | 运行时存档、业务规则、联网素材加载逻辑 | `assets/images/pond_background.png` | 项目静态资源 |
| `.gitignore` | Git 忽略规则 | `node_modules/`, `dist/`, `build/`, logs | 业务配置 | `.gitignore` | Git 项目配置 |
| `README.md` | 项目说明和运行说明 | 当前功能、运行方式、限制 | 夸大宣传、未实现承诺 | `README.md` | 项目文档 |
| `node_modules/` | 本地依赖安装目录 | npm install 自动生成 | 任何手写业务代码、Git 提交 | `node_modules/` | Node/npm 机制，必须忽略 |
| `dist/` / `build/` | 未来打包输出 | 构建产物 | 源码、业务配置 | `dist/` | 构建产物，当前不应存在或不提交 |

---

## 5. 框架推荐 / 框架本身 / 项目自定义标注

### 5.1 Electron / Node 框架机制

| 文件 | 类型 | 说明 |
|---|---|---|
| `main.js` | Electron 主进程 | 创建窗口，管理窗口尺寸，处理主进程 IPC |
| `preload.js` | Electron 安全桥 | 在 contextIsolation 下暴露安全 API |
| `package.json` | Node/npm | 管理依赖与运行脚本 |
| `package-lock.json` | Node/npm | 锁定依赖 |
| `node_modules/` | Node/npm | 依赖目录，不提交 Git |

### 5.2 Web 标准机制

| 文件 | 类型 | 说明 |
|---|---|---|
| `index.html` | HTML 页面入口 | 页面结构 |
| `styles/*.css` | CSS 样式 | 控制视觉 |
| `Canvas` | Web API | 游戏画面绘制 |
| `localStorage` | Web API | 当前单机存档 |

### 5.3 项目自定义业务

| 文件 | 业务 |
|---|---|
| `src/game.js` | 游戏画面、猫猫反应、鱼漂、视觉反馈 |
| `src/fishing.js` | 钓鱼状态机、随机抽鱼 |
| `src/save.js` | 存档、金币、总数、ownedFish |
| `src/bestiary.js` | 图鉴 |
| `src/shop.js` | 商店、升级 |
| `src/disguise.js` | 报表模式 |
| `src/daily-quests.js` | 今日委托 |
| `src/sound.js` | 默认静音的 Web Audio API 轻音效 |
| `src/navigation.js` | 页面切换协调 |
| `src/progression.js` | 摸鱼等级、称号和本地徽章派生展示 |
| `data/fish.json` | 鱼类静态数据 |
| `data/upgrades.json` | 升级静态数据 |
| `assets/images/` | 静态像素美术资源 |

---

## 6. 当前框架下的“请求入口、业务规则、数据访问、校验、错误、权限”在哪里

> 当前项目没有 HTTP 后端。  
> 所以这里的“请求入口”指用户操作入口、渲染进程事件入口、IPC 入口。  
> 未来如果加后端，再使用第 10 节《后端架构规范》。

### 6.1 请求入口

| 场景 | 当前入口 | 文件 |
|---|---|---|
| 用户点击 Canvas 收竿 | Canvas click handler | `src/game.js` / `src/renderer.js` |
| 用户点击功能页 | DOM click handler | `src/renderer.js` 或对应模块 |
| 用户打开图鉴 | 图鉴按钮事件 | `src/bestiary.js` |
| 用户购买升级 | 商店按钮事件 | `src/shop.js` |
| 用户领取委托奖励 | 委托按钮事件 | `src/daily-quests.js` |
| 用户切换迷你模式 | 迷你按钮事件 + IPC | `src/renderer.js`, `preload.js`, `main.js` |
| 窗口尺寸切换 | IPC 主进程处理 | `main.js` |

### 6.2 业务规则

| 规则 | 应放位置 |
|---|---|
| 上钩时间、鱼上钩状态 | `src/fishing.js` |
| 抽鱼权重 | `src/fishing.js` |
| 钓鱼金币收益入账 | `src/save.js`，但收益数值可由调用处传入 |
| 鱼价加成规则 | `src/shop.js` 或统一工具函数；当前避免多处重复计算 |
| 图鉴是否解锁 | `src/bestiary.js` 读取 `ownedFish` |
| 今日委托进度更新 | `src/daily-quests.js` |
| 今日委托领奖 | `src/daily-quests.js` |
| 猫猫反应 | `src/game.js` |
| 稀有鱼视觉提示 | `src/game.js` |
| 音效是否播放及音色 | `src/sound.js`，只监听既有事件，不改事件业务结果 |
| 摸鱼等级和徽章 | `src/progression.js`，优先从已有存档统计派生 |

### 6.3 数据访问

| 数据 | 当前访问方式 | 位置 |
|---|---|---|
| 鱼类配置 | fetch `data/fish.json` | `src/fishing.js` / 相关初始化 |
| 升级配置 | fetch `data/upgrades.json` | `src/shop.js` |
| 存档数据 | localStorage key `bossFishSave` | `src/save.js` |
| 今日委托数据 | 存档内字段 | `src/daily-quests.js` + `src/save.js` |
| 音效开关 | `settings.soundEnabled`，默认 `false` | `src/sound.js` + `src/save.js` |
| v0.3 等级与徽章 | 从已有 `totalCatches`、`coins`、`ownedFish` 等数据派生 | `src/progression.js` |
| UI 状态 | 内存状态 + localStorage settings | `renderer/game/相关模块` |

### 6.4 参数校验

| 参数 | 校验位置 | 校验规则 |
|---|---|---|
| `fish` | `addCatchToSave(fish)` / `recordCatch(fish, earnedCoins)` 调用前后 | 必须有 id/name/rarity/basePrice |
| `earnedCoins` | 传入 `recordCatch` 前 | 必须是非负数字，使用实际获得金币 |
| `questId` | `claimQuest(questId)` | 必须存在于今日任务中 |
| `upgradeId` | 商店购买 | 必须存在于 upgrades 配置 |
| IPC action | `preload.js` / `main.js` | 只能暴露白名单方法 |
| window mode | 切换迷你/普通 | 只允许 normal / mini / report 等已定义值 |

### 6.5 错误处理

| 错误 | 处理方式 |
|---|---|
| fish.json 读取失败 | 显示简短错误，游戏不崩溃 |
| upgrades.json 读取失败 | 商店显示不可用 |
| localStorage 解析失败 | 回退默认存档，标注一次错误 |
| 委托数据缺字段 | 用迁移/默认字段补齐 |
| 领取非法任务 | 不发奖励，返回错误提示 |
| IPC 调用失败 | UI 给出轻提示，不崩溃 |

目前如果没有统一错误模块，后续可以新增：

```text
src/errors.js
```

但只有当错误处理开始重复时才新增，不要提前重构。

### 6.6 权限校验

当前没有账号、用户、角色权限。

当前安全边界主要是 Electron：

| 边界 | 位置 | 规则 |
|---|---|---|
| 渲染进程访问主进程 | `preload.js` | 只通过 contextBridge 暴露白名单 API |
| 窗口尺寸变更 | `main.js` | 只允许已定义尺寸 |
| Node API | 渲染进程禁止直接访问 | 保持 contextIsolation: true |
| 外部链接 / 文件 | 当前不需要 | 不开放 |

未来如果加后端，权限校验必须在后端中间件和业务服务层双重处理。

---

## 7. 最小模块演练：领取今日委托奖励

> 目的：不写完整业务，只看请求链路、分层、文件位置。  
> 当前是 Electron 单机项目，不是 HTTP 后端。

### 7.1 请求从哪进

用户在功能页进入今日委托页，点击“领取”。

```text
UI 点击事件
↓
src/daily-quests.js
claimQuest(questId)
```

入口文件：

```text
src/daily-quests.js
```

对应责任表：

```text
今日委托业务规则和 UI
```

### 7.2 参数在哪里校验

校验点：

```text
claimQuest(questId)
```

必须校验：

```text
questId 是否存在
任务是否 completed === true
任务是否 claimed === false
rewardCoins 是否是非负数字
```

如果失败：

```text
不加金币
不保存 claimed
返回错误或 UI 提示
```

### 7.3 业务规则放哪

业务规则在：

```text
src/daily-quests.js
```

规则：

```text
只有 completed 且未 claimed 的任务可以领取
领取时 coins += rewardCoins
同时 claimed = true
保存必须一次完成
```

不要放在：

```text
src/game.js
src/bestiary.js
src/shop.js
```

### 7.4 数据怎么访问

读取：

```text
src/save.js -> loadSave()
```

修改：

```text
save.coins += rewardCoins
quest.claimed = true
```

保存：

```text
src/save.js -> saveGame(save)
```

注意：

```text
发金币和 claimed = true 必须同一次保存。
```

### 7.5 响应怎么统一

当前无后端 response。

建议函数返回统一对象：

```js
{
  ok: true,
  message: "领取成功",
  data: {
    coins,
    quest
  }
}
```

失败：

```js
{
  ok: false,
  message: "任务尚未完成"
}
```

UI 层只根据 `ok/message` 显示。

### 7.6 错误怎么统一处理

当前最小规则：

```text
业务函数不要 alert
业务函数返回 ok/message
UI 层负责显示 message
console.error 只用于开发错误
```

未来如果错误变多，可新增：

```text
src/result.js
src/errors.js
```

但当前不强制。

### 7.7 权限在哪里校验

当前单机无账号权限。

权限边界是：

```text
不让 renderer 直接访问 Node
不让 UI 绕过 claimQuest 直接改金币
```

所以：

```text
领奖必须走 daily-quests.js 的 claimQuest
不能在按钮点击里直接 save.coins += rewardCoins
```

---

## 8. 最小模块演练：切换迷你模式

### 8.1 请求链路

```text
点击“迷你”
↓
renderer / mini mode UI handler
↓
preload 暴露安全 API
↓
ipcRenderer 调用
↓
main.js ipcMain handler
↓
BrowserWindow setContentSize / setSize / setPosition
↓
渲染进程切换 mini UI
```

### 8.2 参数校验

| 参数 | 校验 |
|---|---|
| mode | 只能是 `normal` 或 `mini` |
| width/height | 只能是预设尺寸，不能传任意值 |
| position | 如果保存拖动位置，需要校验是数字 |

### 8.3 业务规则

```text
迷你模式只改变显示方式
不能重置钓鱼状态
不能清空存档
不能影响今日委托
不能影响图鉴
```

### 8.4 数据访问

```text
settings.windowMode
settings.miniPosition
```

位置：

```text
src/save.js
localStorage bossFishSave
```

### 8.5 错误处理

如果 IPC 失败：

```text
不崩溃
保持普通模式
console.error
UI 可显示轻提示
```

---

## 9. AI 改代码前必须遵守的通用规范

### 9.1 任何代码修改前必须回答

```text
1. 这次要解决什么问题？
2. 会修改哪些文件？
3. 为什么这些文件属于正确位置？
4. 是否涉及框架机制？
5. 是否涉及项目自定义业务？
6. 是否影响现有功能？
7. 如何验收？
8. 哪些项无法自动验证，需要人工验证？
```

### 9.2 不允许的行为

```text
不允许顺手新增未要求功能
不允许跨层乱写业务
不允许在 main.js 写游戏规则
不允许在 preload.js 写业务规则
不允许在 CSS 写状态逻辑
不允许在 game.js 发任务奖励
不允许在 daily-quests.js 重新计算鱼价
不允许在 shop.js 修改图鉴
不允许在 bestiary.js 修改金币
不允许把 node_modules 提交进 Git
不允许没运行就说测试通过
不允许没 commit 就说已存档
```

### 9.3 改规则必须同步文档

如果修改以下内容，必须同步 README 或架构文档：

```text
开发规则
目录结构
存档结构
任务规则
今日委托字段
鱼类字段
升级字段
窗口尺寸
IPC API
错误处理格式
验收标准
```

---

## 10. 《后端架构规范》预留版

> 当前项目没有后端。  
> 以下规范是未来如果引入后端时使用。  
> 在没有正式决定加后端之前，Codex 不得创建 backend、server、database、API、auth 等模块。

### 10.1 什么时候才允许加后端

必须满足至少一个条件：

```text
需要云存档
需要账号登录
需要排行榜
需要跨设备同步
需要在线活动配置
需要多人或社区功能
```

当前不满足，所以不加后端。

### 10.2 如果未来加后端，推荐目录

```text
server/
├─ package.json
├─ src/
│  ├─ app.js
│  ├─ routes/
│  ├─ controllers/
│  ├─ services/
│  ├─ repositories/
│  ├─ validators/
│  ├─ middlewares/
│  ├─ errors/
│  └─ config/
└─ README.md
```

### 10.3 后端目录责任表

| 目录 | 主要职责 | 应放什么 | 禁止放什么 | 示例文件 | 框架机制 |
|---|---|---|---|---|---|
| `server/src/app.js` | 后端应用入口 | app 初始化、中间件注册、路由注册 | 具体业务规则 | `app.js` | 后端框架入口 |
| `server/src/routes/` | 请求路由入口 | URL 到 controller 的映射 | 业务计算、数据库 SQL | `quest.routes.js` | Web 框架路由 |
| `server/src/controllers/` | 请求/响应协调 | 读取参数、调用 service、返回结果 | 复杂业务规则、直接数据库访问 | `quest.controller.js` | 项目自定义 |
| `server/src/services/` | 核心业务规则 | 任务完成、奖励、权限业务判断 | HTTP req/res、SQL 细节 | `quest.service.js` | 项目自定义 |
| `server/src/repositories/` | 数据访问 | 数据库查询、存储读写 | HTTP、业务流程 | `quest.repository.js` | 项目自定义 |
| `server/src/validators/` | 参数校验 | schema、类型校验、范围校验 | 数据库访问、业务写入 | `quest.validator.js` | 项目自定义 |
| `server/src/middlewares/` | 横切逻辑 | auth、error handler、request id | 单个业务实现 | `auth.middleware.js` | Web 框架中间件 |
| `server/src/errors/` | 错误类型和统一响应 | AppError、error codes | 业务数据 | `AppError.js` | 项目自定义 |
| `server/src/config/` | 配置 | env、数据库连接配置 | 业务规则 | `env.js` | 项目自定义 |

### 10.4 后端请求链路规范

以“领取委托奖励”为例：

```text
POST /api/quests/:questId/claim
↓
routes/quest.routes.js
↓
validators/quest.validator.js
↓
middlewares/auth.middleware.js
↓
controllers/quest.controller.js
↓
services/quest.service.js
↓
repositories/quest.repository.js
↓
统一响应
```

### 10.5 后端分层职责

| 层 | 放什么 | 不放什么 |
|---|---|---|
| routes | 路由映射 | 业务规则 |
| validators | 参数合法性 | 数据库访问 |
| controllers | 调 service，组织响应 | 复杂规则 |
| services | 业务规则 | HTTP 细节 |
| repositories | 数据读写 | 业务判断 |
| middlewares | 权限、错误、日志 | 单个业务细节 |
| errors | 统一错误格式 | UI 文案 |

### 10.6 后端统一响应格式

成功：

```json
{
  "ok": true,
  "data": {},
  "message": "success"
}
```

失败：

```json
{
  "ok": false,
  "error": {
    "code": "QUEST_NOT_COMPLETED",
    "message": "任务尚未完成"
  }
}
```

### 10.7 后端参数校验规范

```text
所有外部输入必须先校验。
controller 不直接信任 req.body / req.params / req.query。
validator 只判断参数格式，不做业务规则。
业务规则放 service。
```

### 10.8 后端权限校验规范

```text
身份认证放 middleware。
资源归属校验放 service。
管理员权限和普通用户权限必须区分。
不能只靠前端隐藏按钮。
```

### 10.9 后端数据库访问规范

```text
controller 不能直接访问数据库。
service 不直接写 SQL。
repository 负责数据访问。
数据库错误转成 AppError 或统一错误。
```

### 10.10 后端错误处理规范

```text
所有错误进入统一 errorHandler。
不要在 controller 里到处 try/catch 后返回不同格式。
不要暴露数据库原始错误。
未知错误返回通用错误。
开发环境可以打印详细日志。
```

---

## 11. Codex / AI Git 工作流规范

### 11.1 分支与小里程碑规则

v0.3 起允许“小里程碑分支”：

```text
1. 一个大版本一个分支。
2. 一个大版本里允许包含 3～6 个同主题小需求。
3. 每个需求建议单独 commit。
4. 每个需求必须单独列验收结果。
5. 高风险功能仍然必须单独分支。
6. 不能把互相无关的功能混在一个版本。
7. 涉及开发规则、目录结构、存档结构、任务规则、鱼类字段、升级字段、
   IPC API、窗口尺寸、错误处理格式时，必须同步 README 或架构文档。
8. 当前仍然不做后端、账号、数据库、联网、排行榜、Steam 成就、
   复杂宠物养成、地牢、合成、托盘、全局快捷键、复杂动画系统。
```

普通单功能或高风险功能仍使用独立功能分支。小里程碑只适用于边界明确、
相互关联、能够分别提交和验收的同主题需求。

命名：

```text
v0.2-rare-fish-feedback
v0.2-expand-fish-20
v0.2-cat-reactions
v0.2.1-sound-lite
v0.3-progression-pack
```

禁止：

```text
直接在 main 上开发大功能
互相无关的功能混在一个分支
高风险功能混入小里程碑分支
没有说明小里程碑的 commit 拆分策略
没测试就 merge
```

### 11.2 修改前

```text
git status
git branch --show-current
```

必须确认：

```text
当前分支正确
工作区干净或知道未提交内容是什么
```

### 11.3 修改后

每个小需求完成后必须先单独验收，并建议单独提交：

```powershell
git add 本需求涉及的文件
git commit -m "清晰描述本需求的提交说明"
```

整个版本完成并通过综合回归后：

```powershell
git push
```

如果是新分支：

```powershell
git push -u origin 分支名
```

### 11.4 合并 main

```powershell
git checkout main
git pull origin main
git merge --no-ff 功能分支 -m "merge 功能说明"
npm start
git push
```

### 11.5 打标签

稳定节点：

```powershell
git tag v0.3-stable
git push origin v0.3-stable
```

### 11.6 没验证不能提交“完成”

禁止 commit message：

```text
finish all
done
complete
```

推荐：

```text
add simple cat reactions
fix daily quest task types and rewards
refactor function menu for small window
expand fish collection to 20
```

---

## 12. 下一步建议

当前 `v0.3-progression-pack` 仍是部分完成状态，下一步只能围绕已批准的成长与收集主题推进：

```text
1. 对摸鱼等级、图鉴详情、本地徽章、商店效果说明分别补运行和人工验收证据。
2. 今日总结面板尚未实现；只有在单独方案确认后才能继续开发。
3. 如果新增 todayStats，必须先写明默认值、旧存档迁移、reset save 和重启保留行为。
4. 全部子需求通过后再做金币、今日委托、图鉴、商店、音效和迷你模式综合回归。
5. 综合回归通过后才能讨论合并 main 和创建 v0.3 稳定 tag。
```

### 12.1 当前必须保留的未验证项

```text
1. 本次文档更新没有运行 npm start。
2. 本次文档更新没有重新截图验证 420×260 UI。
3. 本次文档更新没有重新人工听取真实音效。
4. v0.3 四个已有子需求没有在本次文档更新中逐项手动回归。
5. v0.2.5 和 v0.3 当前都没有稳定 tag。
6. v0.3 尚未合并 main。
```

### 12.2 合并与 tag 前置条件

```text
每个子需求有独立验收记录
npm start 有成功证据
涉及 UI 的需求有截图或人工观察记录
localStorage 变更有迁移与重置说明
核心系统综合回归通过
git status 和目标分支明确
```

---

## 13. 给 Codex 的永久提醒

```text
你不是来帮我堆功能的。
你是来帮我小步验证独立小游戏的。

每次改代码前：
先确认分支。
先说明修改文件。
先说明责任归属。
先说明验收方法。

每次改代码后：
必须运行。
必须验收。
必须列未验证项。
每个子需求必须单独列验收结果。
localStorage 变更必须说明默认值、迁移、重置和重启保留。
通过后才允许提交 Git。

没有证据，不要说完成。
```
